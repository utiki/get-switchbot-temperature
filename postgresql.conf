shared_buffers = 15360MB                # メモリの 25%-40% を推奨
work_mem = 300MB                        # 作業メモリ
maintenance_work_mem = 64MB           # VACUUM/インデックス作成用

# チェックポイント
checkpoint_timeout = 10min
checkpoint_completion_target = 0.7

# WAL設定
wal_buffers = 16MB
max_wal_size = 1GB
min_wal_size = 80MB

# パラレル設定
max_worker_processes = 8
max_parallel_workers = 4